/**
 * type annotations and type inferences behave differently for variables, functions and objects.
 * type annotation is when we explicitly mention what type of value a variable will have
 * type inference is when the compiler tries to figure out what type of value a variable refers to
 */
